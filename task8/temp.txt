 


colCoordinate(idx) {}

/**
 * Adjusts the left margin of the parent element based on column width.
 */
handleColumnOffset() {}

/**
 * Sets the zoom level and updates dimensions and rendering accordingly.
 * @param {number} zoom The new zoom level.
 */
setZoom(zoom) {}

/**
 * Draws the column headers on the canvas.
 * @returns {number} The index of the last column drawn.
 */
drawCols() {}

/**
 * Initializes mouse event handlers for resizing columns and rows.
 */
initResizeHandlers() {}

/**
 * Handles mouse movement over the canvas to detect hover over column or row lines.
 * @param {MouseEvent} e The mouse event.
 */
handleMouseMove(e) {}

/**
 * Handles mouse down events to initiate column or row resizing.
 */
handleMouseDown() {}

/**
 * Handles mouse movement during a drag operation to resize columns or rows.
 * @param {MouseEvent} e The mouse event.
 */
handleDragMove(e) {}

/**
 * Handles mouse up events to end column or row resizing.
 */
handleMouseUp() {}

/**
 * Draws a line on the canvas.
 * @param {number} x1 The starting x-coordinate.
 * @param {number} y1 The starting y-coordinate.
 * @param {number} x2 The ending x-coordinate.
 * @param {number} y2 The ending y-coordinate.
 * @param {string} color The color of the line.
 */
drawLine(x1, y1, x2, y2, color) {}

/**
 * Erases a line from the canvas.
 * @param {number} x1 The starting x-coordinate.
 * @param {number} y1 The starting y-coordinate.
 * @param {number} x2 The ending x-coordinate.
 * @param {number} y2 The ending y-coordinate.
 */
eraseLine(x1, y1, x2, y2) {}


     
     add jsdoc comment: eg : 
    /**
    * Intializes the Person object
    *@param {String} firstName FirstName of the person
    *@param {String} lastName Last Name of the person
    *@param {number} age The age of the person
    **/



 * @property {HTMLCanvasElement} mainCanvas - The main canvas DOM element.
 * @property {Object} zoomManager - The zoom manager object.
 * @property {number} totalCanvas - The total number of canvases created.
 * @property {Array<ExcelGrid>} canvases - Array of managed ExcelGrid instances.
 *
 * @method createCanvas - Creates a new ExcelGrid canvas, adds it to the manager, and increments the count.
 * @method zoomAll - Applies the current zoom level to all managed canvases.
